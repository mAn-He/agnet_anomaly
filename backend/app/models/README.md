# Model adapters

Swappable backends: LLM (Gemma), VLM (Phi-4-multimodal / Qwen2.5-VL), OCR (PaddleOCR), anomaly (anomalib PatchCore), embeddings (BGE-M3), reranker, LightGBM.

Phase 1 exposes stub interfaces only; wire real weights in later phases.
